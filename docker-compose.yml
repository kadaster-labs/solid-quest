version: '3.9'

services:
  solid-pod-provider:
    build:
      context: community-server
    command: -p 3001
    environment:
        - CSS_ROOT_FILE_PATH=/data
    ports:
        - 3001:3001
    volumes:
        - ./solid-pod-provider/data:/data
